name: Tests

on:
  push:
  pull_request:

jobs:
  shell-and-dopa-api:
    runs-on: macos-latest
    steps:
      - uses: actions/checkout@v4
      - name: Check shell syntax
        run: sh -n guard/*.sh
      - name: Run dopa API contract test
        run: python3 tests/test_dopa_api.py
